# Verified against the bundled `experiment/` virtualenv (Python 3.13.9).
# Versions are floors, not pins: these are the ones the reported results
# were produced with.
numpy>=2.5.2
pandas>=3.0.5
scikit-learn>=1.9.0
scipy>=1.18.1
matplotlib>=3.11.1
statsmodels>=0.15.0
torch>=2.13.0

# Notebook extras (notebooks/ only; the task scripts do not need these).
# After installing, register the venv as a Jupyter kernel:
#   python -m ipykernel install --user --name sage-experiment \
#       --display-name "Python (experiment)"
ipykernel>=7.3.0
nbformat>=5.11.1
nbclient>=0.11.0

# Needed by every path that touches raw YCB-Video frames: the SAGE
# halves of tasks 3 and 4 (`--dataset_root`), evaluate_sage_robustness.py,
# and task 2's SAGE comparison. The YCB-Video loader reads depth, label
# and colour PNGs through OpenCV. Not needed for the pure-baseline runs,
# which work from baseline_data/*.npz alone.
opencv-python>=4.8
